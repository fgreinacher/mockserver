{
  "schema_version": 1,
  "_comment": [
    "Committed, reviewed absolute performance floors for the daily perf-regression",
    "compare step (.buildkite/scripts/steps/perf-test-compare.sh). Extracted from",
    "that script's jq so a floor can only be loosened by a reviewed diff, never",
    "quietly inside a shell heredoc. The compare step's rolling median + MAD",
    "absorbs noise; these committed floors are what it cannot normalise a slow real",
    "regression below. See docs/plans/performance-programme.md -> 'Budgets that",
    "ratchet'.",
    "",
    "Each budget carries:",
    "  dir      'up'   = higher is worse (latency, CPU, heap, alloc, error rate);",
    "           'down' = lower is worse (throughput). The compare step applies a",
    "                    symmetric floor for whichever direction is set.",
    "  min_pct  minimum relative move from the rolling median that counts as a",
    "           regression when there is no absolute floor (floor == null).",
    "  floor    absolute floor: the threshold never crosses this value however",
    "           quiet the recent window was. null = no absolute floor (relative",
    "           only). For dir 'up' the threshold is max(median+3*sigma, floor);",
    "           for dir 'down' it is min(median-3*sigma, floor).",
    "  gating   true  = a flagged regression FAILS the build (non-zero exit);",
    "           false = reported just as loudly but informational (notify-only)",
    "                   until the metric earns a history-derived budget.",
    "",
    "Wildcard keys 'behaviours.*.<metric>', 'microbench.*.<metric>' and",
    "'microbench_extra.*.<metric>' apply to every runtime behaviour / benchmark key",
    "of that shape (e.g. match_http.p95_ms matches behaviours.*.p95_ms). Every metric",
    "a run emits MUST have a matching budget entry here or the compare step fails",
    "closed (a missing budget must not silently default to 'no floor').",
    "",
    "The 'microbench_extra.*' keys are the promoted dark benchmarks (item 15b):",
    "InboundDecode / LocalCallbackDispatch / ForwardPath / OpenApiValidation /",
    "MetricsIncrement. They OMIT 'gating' on purpose -> non-gating (notify-only):",
    "no baseline history yet, so a flagged regression annotates but must not fail",
    "the build. Give them 'gating': true once each has >=10 clean runs behind it."
  ],
  "budgets": {
    "behaviours.*.p95_ms":              { "dir": "up",   "min_pct": 0.10, "floor": null,  "gating": false },
    "behaviours.*.p99_ms":              { "dir": "up",   "min_pct": 0.10, "floor": null,  "gating": false },
    "behaviours.*.error_rate":          { "dir": "up",   "min_pct": 0,    "floor": 0.005, "gating": false },
    "growth.cpu_ratio":                 { "dir": "up",   "min_pct": 0.10, "floor": 1.30,  "gating": false },
    "growth.heap_ratio":                { "dir": "up",   "min_pct": 0.10, "floor": 1.30,  "gating": false },
    "growth.live_set_bytes":            { "dir": "up",   "min_pct": 0.10, "floor": null,  "gating": false },
    "growth.p95_ratio":                 { "dir": "up",   "min_pct": 0.10, "floor": 2.0,   "gating": false },
    "microbench.*.time_per_op":         { "dir": "up",   "min_pct": 0.05, "floor": null,  "gating": true },
    "microbench.*.alloc_bytes_per_op":  { "dir": "up",   "min_pct": 0.05, "floor": null,  "gating": true },
    "microbench_extra.*.time_per_op":        { "dir": "up",   "min_pct": 0.05, "floor": null },
    "microbench_extra.*.alloc_bytes_per_op": { "dir": "up",   "min_pct": 0.05, "floor": null },
    "peak_achieved_rps":                { "dir": "down", "min_pct": 0.15, "floor": null,  "gating": false },
    "forward.error_rate":               { "dir": "up",   "min_pct": 0,    "floor": 0.01,  "gating": true }
  }
}
