# Resolve the dependency graph (using the imported mockserver-bom) and write it to a file for
# verify.groovy to assert on. dependency:list emits "groupId:artifactId:type:version:scope" lines.
invoker.goals = clean dependency:list -DoutputFile=dependency-list.txt -DappendOutput=false
